# Description:	C++ unit testing library
# URL:		http://sourceforge.net/projects/cppunit
# Maintainer: crux libreoffice team	Alan Mizrahi, alan at mizrahi dot com dot ve
# Depends on:

name=cppunit
version=1.14.0
release=2
source=(https://dev-www.libreoffice.org/src/${name}-${version}.tar.gz)

build() {
  cd $name-$version
  CONFIG_SHELL=/bin/bash \
  ./configure \
    --prefix=/usr \
    --mandir=/usr/share/man \
    --disable-doxygen
  make
  make DESTDIR=$PKG install
  rm -rf $PKG/usr/share/doc
}
